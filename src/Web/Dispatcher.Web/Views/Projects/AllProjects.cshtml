@using Dispatcher.Web.ViewModels.ProjectModels
@model AllProjectsViewModel

<h1>All Projects</h1>

<div class="col-xs-12 col-sm-12 col-md-8 col-lg-9 col-xl-9">
    @foreach (var project in Model.Projects)
    {
        <div class="media tm-related-post">
            @*<div class="media-left media-middle">
                <a asp-controller="Projects" asp-action="Project" asp-route-id="@project.Id">
                    <img class="media-object" src="@project.Url" alt="Generic placeholder image" width="80" height="80">
                </a>
            </div>*@
            <div class="media-body">
                <a asp-controller="Projects" asp-action="Project" asp-route-id="@project.Id"><h4 class="media-heading tm-gold-text tm-margin-b-15">@project.Name</h4></a>
                <p class="tm-small-font tm-media-description">@project.Description</p>
                <p class="tm-small-font tm-media-description">@project.UserRole</p>
                <p class="tm-small-font tm-media-description">@project.Url</p>
            </div>
        </div>
    }
</div>

@* it should be just a view component in to the users prifile page*@